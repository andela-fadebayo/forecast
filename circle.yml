deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:forecast-cee.git $CIRCLE_SHA1:refs/heads/master
      - heroku run rake db:migrate --app forecast-cee:
          timeout: 400 # if your deploys take a long time