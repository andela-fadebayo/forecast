$('#list_item_<%= @todo_item.id %>').remove();
$('.delete-status').html('Item deleted successfully!');

flashCallback = function(_this) {
    $('.delete-status').fadeOut();
};
$('.delete-status').bind('click', function(e) {
    flashCallback();
});
setTimeout(flashCallback, 4000);
