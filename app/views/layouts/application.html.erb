<!DOCTYPE html>
<html>
<head>
  <title>Forecast</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if notice %>
    <p class="alert alert-success"><%= notice %></p>
<% end %>
<% if alert %>
    <p class="alert alert-danger"><%= alert %></p>
<% end %>

<% if guest_signed_in? %>
    Welcome <%= current_guest.first_name %><br />
    You're logged in as <strong><%= current_guest.email %></strong>
    <%= link_to 'Edit Profile', edit_guest_registration_path %> |
    <%= link_to 'Logout', destroy_guest_session_path, method: :delete %>
<% else %>
    <%= link_to 'Register', new_guest_registration_path %> |
    <%= link_to 'Login', new_guest_session_path %> |
<% end %>

<%= yield %>

</body>
</html>
